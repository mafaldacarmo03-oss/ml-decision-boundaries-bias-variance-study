# ml-decision-boundaries-bias-variance-study
This project explores the behavior of machine learning models using synthetic datasets and real data. It compares classifiers such as kNN, logistic regression, LDA, and QDA across different data distributions, and studies bias-variance trade-offs in trees, ensembles, SVMs, and MLPs using the Breast Cancer dataset.
